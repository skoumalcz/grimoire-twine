buildscript {
    ext.versions = [
            'kotlin'       : '1.3.21',
            'gradlePlugin' : '3.4.0-rc03'
    ]

    repositories {
        google()
        jcenter()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }

    dependencies {
        classpath "com.android.tools.build:gradle:${versions.gradlePlugin}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${versions.kotlin}"
        classpath "com.github.skoumalcz:TwineStringsGeneratorPlugin:96a884b2c1"
    }
}

apply plugin: 'kotlin'

allprojects {
    repositories {
        google()
        jcenter()
        mavenLocal()
        mavenCentral()
    }
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}"
}

sourceSets {
    main.java.srcDirs += 'src/main/kotlin'
}
